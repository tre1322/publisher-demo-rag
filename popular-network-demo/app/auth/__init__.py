"""Phase H.1 — auth + multi-tenant.

Modules:
- permissions: role enum + capability predicates
- sessions: token mint/verify + cookie helpers (added in H.1.2)
- middleware: RequireBusinessMiddleware (added in H.1.3)
"""
